<div class="w-full">
  <% if notice.present? %>
    <p class="bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block py-2 px-3"><%= notice %></p>
  <% end %>

  <h1>Courses</h1>

  <div id="courses" class="min-w-full flex flex-wrap justify-center gap-14 pb-6">
    <%= render @courses %>
  </div>
</div>
